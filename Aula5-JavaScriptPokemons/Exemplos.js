/* document.getElementById(desc1) //Retorna o elemento com o id passado
document.getElementsByClassName(desc2) //Retorna um vetor/array com as classes passadas
document.getElementsByTagName(desc3) //Retorna um vetor/array com as tags passadas

//Exemoplos de uso
const titulo = document.getElementById("titulo1")
titulo.textContent = "Título add com JS"
titulo.setAttribute("class", "titulo_base")
titulo.style.color = "red" */